#include <stdio.h>
int somaptr( int a, int *pb){
    return a + *pb;
}
/* todas as chamadas estao erradas, primeiro deve haver um inteiro e dps um ponteiro, e isso n acontece em nenhuma alternativa*/